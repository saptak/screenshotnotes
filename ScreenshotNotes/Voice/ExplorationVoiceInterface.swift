
import Foundation

class ExplorationVoiceInterface {
    // This class will handle voice commands for the Exploration Mode.
    // For now, it's a placeholder.

    func handle(command: String) {
        print("Received exploration command: \(command)")
    }
}
